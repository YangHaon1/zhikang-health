# 智康健康管理系统 · 大学生竞赛优化方案

> **定位**：面向高校师生的慢病风险早筛、分层预警与连续健康干预平台。
>
> **本文件用途**：在既有 B0→B10 后端化改造完成的基础上，定义竞赛版本的后续优化范围、阶段交付物与验收标准。除非另行确认，本文是规划文档，**不自动触发后续功能开发**。

---

## 一、项目目标与边界

### 1.1 竞赛目标

将当前的「个人健康记录与风险分析系统」升级为可完整演示的健康干预闭环：

```text
多源数据采集 → 数据质量校验 → 风险识别 → 分层预警
→ 个性化行动计划 → 打卡随访 → 效果评估 → 脱敏群体洞察
```

答辩中需清楚回答四个问题：

1. 服务谁：高校学生、教职工及校医院/健康管理人员。
2. 解决什么问题：久坐、作息不规律与慢病风险发现晚、干预难坚持。
3. 为什么可信：规则依据可追溯、AI 有安全边界、数据质量可说明。
4. 为什么可落地：本地规则引擎可离线运行，前后端一体部署，数据按用户隔离。

### 1.2 明确不做的事

- 不宣称诊断、处方或替代医生；系统定位为健康管理与风险提示。
- 不在竞赛阶段直接接入真实医院生产系统或未授权的个人健康数据。
- 不通过扩充无关功能（商城、社交广场、泛运动课程）来堆砌功能数。
- 不破坏现有 B0→B10 的接口兼容性、`user_id` 数据隔离、规则引擎单一源码约束。

---

## 二、当前能力盘点

| 能力 | 当前基础 | 竞赛评价 |
| --- | --- | --- |
| 真实后端 | Express + SQLite + JWT + bcrypt + Docker | 已具备落地基础 |
| 健康数据 | 档案、指标录入、Excel 导入、趋势、导出 | 仍偏手动/管理端数据来源 |
| 风险分析 | 单一共享规则引擎、评分、风险点、就医提醒 | 有可信内核，需产品化解释 |
| AI | 本地规则问答 + 可选大模型 + 历史持久化 | 有亮点，需强化安全边界 |
| 管理端 | 用户、导入、种子数据 | 缺少脱敏群体健康洞察 |
| 隐私 | JWT、bcrypt、接口鉴权、用户隔离 | 缺少用户可见的授权与数据权利 |

---

## 三、产品主线与核心页面

### 3.1 用户角色

| 角色 | 核心诉求 | 主要页面 |
| --- | --- | --- |
| 学生/教职工 | 快速记录、理解风险、完成计划 | 今日健康、快速录入、行动计划、报告对比 |
| 健康管理员 | 发现需关注群体、组织健康活动 | 脱敏群体看板、导入、随访任务 |
| 校医/指导老师（后续） | 在用户授权下查看限定数据、给出随访建议 | 授权列表、随访记录、报告查看 |

### 3.2 演示主流程

1. 用户完成基础档案并导入/同步健康数据。
2. 系统识别血压、BMI、血糖等风险，说明使用了哪些数据和规则。
3. 用户获得 7 天行动计划并完成每日打卡。
4. 系统展示前后指标和风险等级变化，生成可追溯报告。
5. 管理员查看脱敏群体风险分布，发起健康活动或随访。

---

## 四、阶段计划

> 每阶段独立提交、先验证再进入下一阶段；接口均延续 `{ code, message, data }` 统一响应。

### C0：规则可解释性与医疗安全底座（最高优先级）

**目标**：让风险结论可解释、有边界、可追溯。

**功能范围**

- 在 `server/shared/health-engine.ts` 为每条规则补充 `ruleId`、`ruleVersion`、适用人群、阈值说明与依据链接字段。
- 新增风险解释接口：返回本次结论使用的指标、记录日期、阈值、建议和限制说明。
- 建立 `emergency` 规则：高风险指标或危险症状关键词优先返回固定的紧急处置卡，不交由大模型自由回答。
- 在报告、AI 对话和风险页固定展示“健康管理提示，不能替代医生诊断”的边界文案。

**验收标准**

- 每一项风险结果都能显示规则版本、阈值、数据来源和记录日期。
- 危急关键词/极端指标触发固定安全提示；大模型模式不可绕过。
- 单元测试覆盖所有阈值临界值及高风险分流。

### C1：健康行动计划与打卡闭环（最高优先级）

**目标**：从“给建议”升级为“帮助用户完成建议”。

**数据模型**

```sql
CREATE TABLE health_plans (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id),
  title TEXT NOT NULL,
  risk_key TEXT NOT NULL,
  target_value REAL,
  start_date TEXT NOT NULL,
  end_date TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'active',
  source TEXT NOT NULL DEFAULT 'rule',
  create_time TEXT DEFAULT (datetime('now','localtime'))
);

CREATE TABLE plan_tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  plan_id INTEGER NOT NULL REFERENCES health_plans(id),
  title TEXT NOT NULL,
  task_type TEXT NOT NULL,
  frequency TEXT NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0
);

CREATE TABLE task_checkins (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id),
  task_id INTEGER NOT NULL REFERENCES plan_tasks(id),
  value TEXT DEFAULT '',
  note TEXT DEFAULT '',
  checked_date TEXT NOT NULL,
  create_time TEXT DEFAULT (datetime('now','localtime')),
  UNIQUE(user_id, task_id, checked_date)
);
```

**接口范围**

- `GET/POST /api/health/plans`
- `GET/PUT /api/health/plans/:id`
- `POST /api/health/tasks/:id/checkin`
- `GET /api/health/plans/today`
- `GET /api/health/plans/:id/progress`

**前端范围**

- 首页增加“今日待完成”和连续打卡天数。
- 新增“行动计划”页面，展示风险目标、每日任务、完成度和阶段反馈。
- 报告页增加“本周期计划完成率”和下周期建议。

**验收标准**

- 任意计划、任务、打卡均强制按 `user_id` 隔离。
- 同一任务每日仅能成功打卡一次。
- 计划进度与报告中的完成率一致。

### C2：数据来源、质量与模拟设备同步（高优先级）

**目标**：提升数据真实性与结论可信度，避免竞赛演示只像“填表系统”。

**数据模型扩展**

```sql
ALTER TABLE records ADD COLUMN measured_at TEXT;
ALTER TABLE records ADD COLUMN source_type TEXT DEFAULT 'manual';
ALTER TABLE records ADD COLUMN source_name TEXT DEFAULT '';
ALTER TABLE records ADD COLUMN measurement_state TEXT DEFAULT 'unknown';
ALTER TABLE records ADD COLUMN quality_flag TEXT DEFAULT 'valid';
ALTER TABLE records ADD COLUMN device_id TEXT DEFAULT '';
```

**功能范围**

- 录入时选择来源：手动、Excel、设备同步、校医院测量。
- 增加测量场景：静息、运动后、餐前、餐后、未知。
- 新增“模拟设备同步”演示入口，导入统一 JSON/CSV 结构，不依赖比赛现场网络。
- 质量规则：合理范围、重复记录、异常跳变、测量条件缺失。
- 所有趋势/报告展示来源标签和数据质量提示。

**验收标准**

- 不合理值不直接进入正式分析；需用户确认或标记为可疑。
- 图表与报告可展示数据来源和有效记录占比。
- 模拟同步可在离线环境下完成完整演示。

### C3：报告对比与效果评估（高优先级）

**目标**：证明干预是否有效，而不是只给单次评分。

**功能范围**

- 报告详情支持任选两期进行对比。
- 展示指标变化、风险等级变化、计划完成率及改善/恶化原因。
- 报告写入规则版本、数据截止时间、有效数据量和质量说明。
- 增加“健康历程”时间轴：记录、风险事件、计划、打卡、报告。

**验收标准**

- 两份报告对比可正确显示变化值、变化方向和数据周期。
- 删除或跨用户访问报告仍返回 404，不能泄露数据。

### C4：脱敏群体健康看板（高优先级）

**目标**：扩展校医院/管理员场景，同时避免将个人隐私暴露给管理者。

**功能范围**

- 新增管理员“群体健康洞察”页面：用户活跃、档案完善、风险分布、指标趋势、计划完成率。
- 按学院/部门/年级展示时仅允许聚合统计；样本数低于阈值（建议 10）不展示。
- 默认不展示姓名、联系方式、原始指标明细。
- 导出仅支持脱敏汇总数据，记录管理员导出操作。

**验收标准**

- 普通用户访问一律 403。
- 小样本分组被隐藏或合并为“其他”。
- 统计查询无跨用户明细泄露，SQL 全部参数化。

### C5：用户授权、隐私和审计（中高优先级）

**目标**：从“系统管理员可见”升级为“用户自主授权”。

**数据模型**

```sql
CREATE TABLE health_shares (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  owner_user_id INTEGER NOT NULL REFERENCES users(id),
  grantee_user_id INTEGER NOT NULL REFERENCES users(id),
  scopes TEXT NOT NULL,
  expires_at TEXT,
  status TEXT NOT NULL DEFAULT 'active',
  create_time TEXT DEFAULT (datetime('now','localtime'))
);

CREATE TABLE audit_logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  actor_user_id INTEGER,
  action TEXT NOT NULL,
  target_type TEXT NOT NULL,
  target_id TEXT NOT NULL,
  detail TEXT DEFAULT '',
  create_time TEXT DEFAULT (datetime('now','localtime'))
);
```

**功能范围**

- “我的数据”页面：导出、删除申请、隐私说明。
- “授权中心”：限定档案/记录/报告范围、设置有效期、一键撤销。
- 关键操作写审计日志：登录、导出、分享、撤销、管理员操作。

**验收标准**

- 授权过期或撤销后立即拒绝访问。
- 用户只可导出自己的数据；管理员导出走脱敏汇总路径。

### C6：移动端、提醒与可访问性（中优先级）

**目标**：让系统从后台 Web 更接近真实健康管理产品。

**功能范围**

- 移动端首页仅保留：今日风险、快速记录、今日任务、趋势摘要。
- PWA 安装、离线草稿、网络恢复后再同步。
- 浏览器提醒：测量、打卡、报告生成；默认关闭并由用户明确授权。
- 色盲友好：风险提示不只使用颜色，必须同时使用文字、图标和等级。
- 大字号、键盘操作、图表替代文本。

**验收标准**

- 375px 宽度下核心录入与打卡无横向滚动。
- 无色彩信息时仍可区分风险等级。

### C7：测试、可观测性与部署演练（贯穿全部阶段）

**目标**：用证据支撑竞赛答辩，不只展示功能。

**功能范围**

- 为规则引擎、权限、计划打卡、授权过期、脱敏统计补充 Vitest 测试。
- 保留并扩展 API 回归脚本，覆盖新增接口与跨用户越权用例。
- 增加结构化日志、请求 ID、错误码说明、数据库备份/恢复演练。
- 实际完成 Docker 构建、容器重启持久化、离线模式演练。

**验收标准**

- `pnpm typecheck`、前后端测试、构建、API 回归均通过。
- Docker 在干净环境可一条命令启动；重启后数据存在。
- 答辩材料能给出测试数量、成功率、性能数据与隐私策略。

---

## 五、技术约束与实现原则

1. **规则引擎唯一源码**：继续以 `server/shared/health-engine.ts` 为唯一健康规则来源；前端只能导入纯函数。
2. **数据隔离优先**：所有新增表均带 `user_id` 或通过父实体关联到用户；读取、更新、删除都在 SQL 条件中校验归属。
3. **隐私默认保护**：群体分析默认聚合，分享默认关闭，提醒默认关闭。
4. **AI 不越权**：AI 只做健康教育、数据解释和任务引导；高风险处理由确定性规则优先。
5. **离线可演示**：关键路径不得依赖大模型、第三方云接口或可穿戴设备网络。
6. **先兼容再迭代**：保留现有接口与页面功能，在新接口稳定后逐步替换调用方。

---

## 六、竞赛材料清单

### 6.1 演示数据与脚本

- 三个完整人物档案：健康维持、风险改善、高风险转介。
- 每个档案至少 30 天数据、计划打卡、两期报告与解释链路。
- 5 分钟演示脚本：问题背景 → 风险识别 → 行动计划 → 结果改善 → 群体洞察 → 隐私保障。

### 6.2 答辩证据

- 系统架构图、数据流图、权限与授权流程图。
- 规则依据页、AI 安全边界页、隐私设计页。
- 测试报告：规则边界、接口回归、越权阻断、容器重启持久化。
- 可用性数据：任务完成率、风险识别准确率（基于标注演示集）、页面响应时间。

### 6.3 评价指标

| 指标 | 建议目标 |
| --- | --- |
| 档案完善率 | ≥ 80%（演示样本） |
| 风险用户计划生成率 | 100% |
| 每日任务完成率 | ≥ 70%（演示周期） |
| 高风险规则召回率 | 100%（标注测试集） |
| 越权访问阻断率 | 100% |
| 核心页面移动端可用性 | 375px 宽度通过人工验收 |

---

## 七、建议实施顺序

| 顺序 | 阶段 | 预计投入 | 竞赛价值 |
| --- | --- | --- | --- |
| 1 | C0 规则可解释与安全 | 2–3 天 | 高：建立可信度 |
| 2 | C1 行动计划与打卡 | 4–6 天 | 最高：形成闭环 |
| 3 | C3 报告对比与效果评估 | 2–3 天 | 高：展示成果 |
| 4 | C4 脱敏群体看板 | 3–4 天 | 高：体现社会价值 |
| 5 | C2 数据来源与模拟同步 | 3–5 天 | 高：提升真实感 |
| 6 | C5 隐私授权与审计 | 3–4 天 | 中高：体现专业性 |
| 7 | C6 移动端与提醒 | 3–5 天 | 中：提升体验 |
| 8 | C7 测试与部署演练 | 贯穿执行 | 必须：支撑交付 |

如时间有限，建议完成 **C0 + C1 + C3 + C4 + C7**，即可形成完整且差异化的竞赛版本。
