# 智康健康管理系统 · 5 分钟演示脚本

> 建议赛前演练 2~3 遍，总时长控制在 5 分钟内。

## 开演前 3 分钟准备（本机）

```bash
pnpm start          # = pnpm build && tsx server/src/index.ts
# 浏览器打开 http://localhost:3000（单端口：前端 + 接口 + 头像）
```

1. 用 `admin` / `admin123` 登录，首页点「一键生成演示数据」（约 1 秒写入 90 天记录）。
2. 确认首页风险提示条已有内容，然后刷新一次页面回到干净状态。
3. 断网演示更稳妥：拔掉网线 / 关掉 WiFi 也不影响（方案 A 规则引擎全在本地）。

> 换机器演示：服务端 `0.0.0.0:3000` 已监听，同一局域网内用 `http://本机IP:3000` 即可，数据都在服务端的 `server/data/zhikang.db`，换浏览器看到的是同一份数据。

## 演示流程

| 步骤                  | 时长 | 操作                                                                                                                     | 讲解要点                                                                              |
| --------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- |
| 1. 登录               | 20s  | 打开系统，用 `admin` / `admin123` 登录                                                                                   | 账号体系：admin 管理员 / common 普通用户；密码 bcrypt 入库、JWT 签发，菜单随角色收敛  |
| 2. 健康总览           | 40s  | 首页展示核心指标卡片、近 30 天趋势、风险提示条                                                                           | 重点讲「综合风险等级」与「主要风险点」，体现 AI 分析价值                              |
| 3. 一键演示数据       | 10s  | 若无数据，点击首页「一键生成演示数据」                                                                                   | 说明 90 天模拟记录、含 2~3 个异常指标，数据落 SQLite、重启不丢                        |
| 4. 趋势分析           | 40s  | 进入「趋势分析」，切换血压 / 血糖等指标与时间范围                                                                        | 展示绿色「正常参考范围带」与指标波动                                                  |
| 5. 生成报告并导出 PDF | 60s  | 进入「AI 健康报告」，选最近 30 天生成，点「导出 PDF」                                                                    | 讲评分卡、雷达图、分项分析、风险点、就医提醒（每用户最多留 20 份）                    |
| 6. AI 对话            | 60s  | 进入「AI 健康助手」，问「我的健康风险」「分析一下我的血压」；切到「AI 大模型」看降级提示                                 | 强调方案 A（规则引擎）离线可用、断网也能演示；对话历史存服务端，重启/换浏览器仍可恢复 |
| 7. 收尾（可选）       | 40s  | 退出换 `common` 登录看菜单差异 → 回来用 admin 演示「用户管理」（新增用户 / 重置密码 / 分配角色 / 上传头像）与 Excel 导入 | 体现 RBAC 权限（common 调管理接口返回 403）+ 数据导入 + 用户管理能力                  |

## 备选演示数据口径

- 演示数据：按本地日期生成 90 天记录，最近 12 天为「异常演示段」——收缩压 150~~165 / 舒张压 90~~100（高血压）、空腹血糖 7.0~~7.6（疑似糖尿病）、LDL 4.1~~4.5（升高）；前 78 天为正常段。
- 演示档案（自动生成）：45 岁男、172cm/78kg、BMI≈26.4 超重、吸烟/饮酒偶尔、几乎不运动 → 触发 BMI 分级 + 生活方式风险 + 就医提醒（最近 3 次血压 ≥140/90）。
- 演示数据生成器唯一源码：`server/shared/health-seed.ts`（前端不复制一份，规则口径前后端一致）。

## 常见提问的应答口径

| 提问                 | 回答                                                                                                                                                        |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 数据存在哪？         | 服务端 SQLite（`server/data/zhikang.db`）+ 上传头像在 `server/data/uploads`，重启进程不丢；Docker 下挂 volume                                               |
| AI 是怎么做的？      | 双通道：A 本地医学规则引擎（唯一源码 `server/shared/health-engine.ts`，前后端共用同一套分级口径）；B 真实大模型（后端直连火山方舟，Key 只在 `server/.env`） |
| 怎么保证数据不串号？ | 所有健康数据读写强制 `WHERE user_id = 当前登录用户`，跨用户访问返回 404；管理接口加 `adminOnly`（非管理员 403）                                             |
| 密码安全吗？         | bcrypt 哈希入库（明文不落库），登录签发 JWT（accessToken 12h / refreshToken 7d）                                                                            |
