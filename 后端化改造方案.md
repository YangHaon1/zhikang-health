# 智康健康管理系统 · 后端化改造方案

> 目标：从「纯前端 + Mock」升级为「前端 + Node 真实后端 + SQLite 持久化」，实现真多用户、跨设备数据持久、服务端鉴权，并预留云端部署能力。
> 技术选型（已确认）：**Node.js + TypeScript + Express + SQLite（better-sqlite3）**；Mock 彻底删除；部署先本机、后续可上云。
> 执行方式：按 B0→B10 阶段推进，每阶段独立 commit、可回退、可验证；本方案文档为唯一权威依据。

---

## 一、现状与改造边界

### 现状

- 所有接口来自 `mock/`（Vite 中间件 + 生产浏览器 XHook 注入），数据存浏览器 localStorage + 内存兜底。
- 局限：数据锁在单浏览器、换设备丢失、多用户共享同一份数据、登录不校验密码（假鉴权）、GitHub Pages 静态托管无法承载真实后端。

### 改造后

- 数据落在服务端 SQLite 单文件 `server/data/zhikang.db`，**换设备/换浏览器/重启服务数据都在**。
- 登录为真实 JWT 鉴权（admin / common 各设密码），健康数据按 `user_id` 隔离，真多用户。
- AI 方案 A（规则引擎）迁到服务端；方案 B（大模型）由服务端直连，API Key 不再暴露给前端。
- 生产形态：`pnpm build` 后由 Express 托管前端静态文件 + `/api`，**一个进程一个端口跑全站**；本机演示 `pnpm start` 一条命令。
- 云端预留：Dockerfile + docker-compose + `.env.example`，后续部署到任意云服务器。

### 边界（不做的事）

- 不做复杂 RBAC 表（沿用 admin / common 两角色 + 路由 roles 过滤）。
- 不做用户注册开放入口（默认种子账号，管理员可增删改用户）。
- 不迁移旧 localStorage 数据（演示场景直接重新一键生成）。
- 不加消息队列/WebSocket 等重型设施。

---

## 二、总体架构

```
浏览器（任意设备）                      Node 服务（localhost:3000）
┌──────────────────┐   HTTP /api   ┌──────────────────────────────┐
│ Vue 3 前端        │ ────────────▶ │ Express 应用                  │
│ · 登录/对话/报告  │               │ · 鉴权中间件（JWT）           │
│ · 实时分级标签    │ ◀──────────── │ · 路由：auth/user/health/...  │
│ （轻量 grade）    │               │ · 规则引擎（AI 方案 A）        │
└──────────────────┘               │ · 大模型通道（AI 方案 B）      │
                                   │ · 静态托管 dist（生产）        │
                                   └──────────────┬───────────────┘
                                                  │ better-sqlite3
                                   ┌──────────────▼───────────────┐
                                   │ SQLite：data/zhikang.db       │
                                   │ users/profiles/records/       │
                                   │ reports/chat_history          │
                                   └──────────────────────────────┘
```

### 新增目录结构

```
server/
├── package.json            # 后端独立依赖（express/sqlite/jwt...）
├── tsconfig.json
├── .env.example            # PORT / JWT_SECRET / LLM_API_KEY
├── data/                   # 运行时生成 zhikang.db（gitignore）
├── shared/
│   └── health-engine.ts    # ★ 规则引擎唯一源码（前后端共用）
└── src/
    ├── index.ts            # Express 入口 + 静态托管 + SPA 回退
    ├── db.ts               # SQLite 连接 + 建表 + 种子数据
    ├── middleware/auth.ts  # JWT 解析 / admin 校验
    ├── routes/
    │   ├── auth.ts         # login / refresh-token / mine / get-async-routes
    │   ├── user.ts         # 用户管理（admin）+ role/dept 下拉
    │   ├── health.ts       # 档案/记录/导入/导出/分析/报告/对话/seed
    │   └── upload.ts       # 头像上传（multer）
    └── data/
        └── asyncRoutes.ts  # 动态路由表（从 mock/asyncRoutes.ts 迁移）
```

### 前端改动点

| 文件                                                      | 改动                                                                                 |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| `src/api/health.ts` / `user.ts` / `system.ts` / `mock.ts` | 路径加 `/api` 前缀（白名单 `endsWith` 兼容，http.ts 不用动）                         |
| `src/utils/http.ts`                                       | 不改；`/api/login`、`/api/refresh-token` 仍命中白名单                                |
| `src/utils/auth.ts`                                       | 不改；token 结构兼容（accessToken/refreshToken/expires）                             |
| 录入页实时分级                                            | import 改 `@shared/health-engine`（alias 指向 `server/shared`），仅用分级函数        |
| `vite.config.ts`                                          | `server.proxy` 加 `/api → http://localhost:3000`；删除 `/llm-api` 代理（改后端直连） |
| `build/plugins.ts`                                        | 删除 `vitePluginFakeServer`；`package.json` 删除 `vite-plugin-fake-server`           |
| `mock/` 目录                                              | 整目录删除                                                                           |

---

## 三、数据库设计（SQLite）

```sql
-- 用户表
CREATE TABLE users (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  username      TEXT NOT NULL UNIQUE,
  password_hash TEXT NOT NULL,            -- bcrypt
  nickname      TEXT DEFAULT '',
  email         TEXT DEFAULT '',
  phone         TEXT DEFAULT '',
  description   TEXT DEFAULT '',
  avatar        TEXT DEFAULT '',
  roles         TEXT DEFAULT 'common',    -- 'admin' | 'common'
  create_time   TEXT DEFAULT (datetime('now','localtime'))
);

-- 健康档案（一用户一条）
CREATE TABLE profiles (
  user_id    INTEGER PRIMARY KEY REFERENCES users(id),
  name       TEXT DEFAULT '',
  gender     TEXT DEFAULT 'male',        -- male | female
  birth_date TEXT DEFAULT '',
  height     REAL,                        -- cm
  weight     REAL,                        -- kg
  smoking    INTEGER DEFAULT 0,           -- 0否 1是
  drinking   INTEGER DEFAULT 0,           -- 0否 1偶尔 2经常
  exercise   INTEGER DEFAULT 0,           -- 0不足 1一般 2良好
  update_time TEXT DEFAULT (datetime('now','localtime'))
);

-- 健康记录（指标）
CREATE TABLE records (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id            INTEGER NOT NULL REFERENCES users(id),
  record_date        TEXT NOT NULL,       -- yyyy-MM-dd
  systolic           INTEGER,             -- 收缩压
  diastolic          INTEGER,             -- 舒张压
  fasting_glucose    REAL,                -- 空腹血糖
  postprandial_glucose REAL,              -- 餐后血糖
  total_cholesterol  REAL,                -- 总胆固醇
  triglycerides      REAL,                -- 甘油三酯
  ldl                REAL,                -- 低密度脂蛋白
  hdl                REAL,                -- 高密度脂蛋白
  heart_rate         INTEGER,             -- 心率
  blood_oxygen       INTEGER,             -- 血氧
  weight             REAL,                -- 体重
  note               TEXT DEFAULT '',
  create_time        TEXT DEFAULT (datetime('now','localtime'))
);
CREATE INDEX idx_records_user_date ON records(user_id, record_date DESC);

-- 报告
CREATE TABLE reports (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id       INTEGER NOT NULL REFERENCES users(id),
  period_start  TEXT, period_end TEXT,
  score         INTEGER, level TEXT,
  summary       TEXT,                    -- JSON：总体评价/分项分析/风险点/建议/就医提醒
  radar         TEXT,                    -- JSON：雷达图数据
  trend         TEXT,                    -- JSON：趋势图数据
  create_time   TEXT DEFAULT (datetime('now','localtime'))
);

-- 对话历史
CREATE TABLE chat_history (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id     INTEGER NOT NULL REFERENCES users(id),
  role        TEXT NOT NULL,             -- 'user' | 'assistant'
  content     TEXT NOT NULL,
  create_time TEXT DEFAULT (datetime('now','localtime'))
);
```

种子数据：`admin / admin123`（roles=admin）、`common / common123`（roles=common），密码 bcrypt 哈希入库。

---

## 四、接口清单（对齐现有前端）

| 方法            | 路径                                                  | 说明                                                                    | 权限  |
| --------------- | ----------------------------------------------------- | ----------------------------------------------------------------------- | ----- |
| POST            | `/api/login`                                          | 用户名+密码 → `{accessToken, refreshToken, expires, 用户信息(含roles)}` | 公开  |
| POST            | `/api/refresh-token`                                  | 刷新令牌                                                                | 公开  |
| GET             | `/api/mine`                                           | 当前用户信息（账户设置）                                                | 登录  |
| GET             | `/api/mine-logs`                                      | 安全日志（示例数据）                                                    | 登录  |
| GET             | `/api/get-async-routes`                               | 按 roles 过滤后的动态路由                                               | 登录  |
| GET             | `/api/user`                                           | 用户列表（分页）                                                        | admin |
| POST/PUT/DELETE | `/api/user` `/api/user/:id`                           | 新增/修改/删除用户                                                      | admin |
| GET             | `/api/list-all-role` `/api/list-role-ids` `/api/dept` | 用户管理页下拉                                                          | admin |
| POST            | `/api/upload`                                         | 头像上传（multer，存 server/data/uploads）                              | 登录  |
| GET/PUT         | `/api/health/profile`                                 | 档案读取/保存                                                           | 登录  |
| GET             | `/api/health/records`                                 | 分页 + 日期范围筛选（按 user_id）                                       | 登录  |
| POST            | `/api/health/records`                                 | 新增记录                                                                | 登录  |
| PUT/DELETE      | `/api/health/records/:id`                             | 修改/删除（校验归属）                                                   | 登录  |
| POST            | `/api/health/records/import`                          | Excel 逐行校验批量入库                                                  | admin |
| GET             | `/api/health/records/export`                          | 全量导出（前端 xlsx 生成文件）                                          | 登录  |
| POST            | `/api/health/analyze`                                 | 规则引擎分析（评分/分级/建议）                                          | 登录  |
| POST            | `/api/health/report/generate`                         | 生成报告（时间段过滤，最多留 20 份）                                    | 登录  |
| GET             | `/api/health/report/history`                          | 报告历史                                                                | 登录  |
| GET             | `/api/health/report/:id`                              | 报告详情（校验归属）                                                    | 登录  |
| POST            | `/api/health/chat`                                    | 对话（A 规则引擎意图匹配 / B 大模型直连）                               | 登录  |
| POST            | `/api/health/seed`                                    | 一键生成 90 天演示数据                                                  | admin |

响应统一：`{ code: 0, message: "操作成功", data: ... }`（与现状一致，前端零适配成本）。

---

## 五、关键机制

### 1. 鉴权（JWT）

- 登录成功签发 `accessToken`（含 userId、roles，有效期 12h）+ `refreshToken`（有效期 7d）。
- 中间件 `authMiddleware`：解析 `Authorization: Bearer xxx` → `req.user`；失败返回 401（前端 http.ts 已有刷新/登出流程）。
- `adminOnly` 中间件：校验 `req.user.roles` 含 `admin`。

### 2. 动态路由

- `server/src/data/asyncRoutes.ts` 从现有 `mock/asyncRoutes.ts` 原样迁移（数据结构不变）。
- `/api/get-async-routes` 按 `req.user.roles` 过滤（复用现有 `filterNoPermissionTree` 逻辑的服务端版本）。

### 3. 规则引擎共享（保持"单份源码"卖点）

- 规则引擎唯一源码放 **`server/shared/health-engine.ts`**（纯函数、零 Node 依赖）。
- 后端：直接 `import`（analyze / report / chat 全用它）。
- 前端：`vite.config.ts` alias 加 `"@shared": pathResolve("server/shared")`，`tsconfig.json` paths 同步；录入页实时分级 `import { gradeSystolic } from "@shared/health-engine"`。
- 好处：分级口径前后端永远一致，无双写。

### 4. AI 双通道

- **方案 A（默认）**：后端规则引擎意图匹配（复用现有 `/health/chat` 逻辑：意图→查档案+最近 5 条→analyzeHealth→拼接回答）。
- **方案 B**：后端 `fetch` 火山方舟 API（`/api/v3/chat/completions`），Key 存 `server/.env` 的 `LLM_API_KEY`，前端不再持有任何 Key；保留 A/B 切换（localStorage `health-ai-mode`），无 Key 时降级提示。

### 5. 数据隔离与安全

- 所有健康查询/写操作强制 `WHERE user_id = req.user.id`；跨用户访问返回 404。
- SQL 全部参数化（better-sqlite3 prepare 天然防注入）。
- 密码 bcrypt 哈希；JWT_SECRET 从环境变量读取。

---

## 六、阶段计划（每阶段独立 commit）

> 通用验收基线：`pnpm typecheck`（前端）EXIT=0；`curl` 实测接口；`git` 干净。

### B0 后端骨架

- 新建 `server/`（package.json、tsconfig、`src/index.ts` 最小 Express + `/api/ping`）。
- 根 `package.json` 加脚本：`dev:server: tsx watch server/src/index.ts`、`start: node server/src/index.ts`（或 dist）；根依赖加 `tsx`（dev）。
- `vite.config.ts` 加 `server.proxy["/api"] → http://localhost:3000`（保留 `/llm-api` 暂不动）。
- `.gitignore` 加 `server/data/`、`server/.env`。
- **验收**：`pnpm dev:server` 起服务，`curl http://localhost:3000/api/ping` 返回 ok；前端 dev 下 `fetch('/api/ping')` 经代理通。

### B1 数据库与种子

- `server/src/db.ts`：建表（上文 SQL）+ 种子（admin/common 账号 bcrypt 入库、dept/role 常量）。
- `server/.env.example`：`PORT=3000`、`JWT_SECRET=`（随机串）、`LLM_API_KEY=`。
- **验收**：启动后生成 `zhikang.db`，表齐全；`SELECT * FROM users` 两条种子记录。

### B2 鉴权与登录打通

- 实现 `login / refresh-token / mine / mine-logs / get-async-routes`。
- `asyncRoutes.ts` 迁移 + roles 过滤。
- 前端 `src/api/user.ts`（登录接口）路径改 `/api/login` 等；本地登录页可正常登录。
- **验收**：浏览器 admin/admin123 登录成功、菜单按角色加载；curl 非法密码返回错误；无 token 访问 /api/mine 返回 401。

### B3 档案 + 记录 CRUD

- `routes/health.ts`：profile（GET/PUT）、records（分页 GET、POST、PUT/DELETE :id），全部按 user_id 隔离。
- 前端 `src/api/health.ts` 全路径加 `/api`；录入/历史/编辑/删除页打通。
- **验收**：curl 全链路（增查改删、分页、日期筛选、跨用户访问 404）；浏览器操作正常。

### B4 规则引擎搬迁 + 实时分级

- 移动 `src/utils/health-engine.ts` → `server/shared/health-engine.ts`（内容不动）。
- 前端 alias `@shared` + tsconfig paths；录入页实时分级 import 改 `@shared/health-engine`（仅分级函数，勿引 Node API）。
- `/api/health/analyze` 实现（复用现有逻辑）。
- **验收**：curl analyze 全异常数据评分/等级正确；前端录入页红黄绿标签正常；`pnpm typecheck` 通过。

### B5 报告接口

- `/api/health/report/generate|history|:id`（时间段过滤、JSON 字段存 reports 表、最多留 20 份）。
- 前端报告页切真实接口；导出 PDF（打印）/ Excel（前端 xlsx）保留。
- **验收**：seed 后生成报告，评分/雷达/趋势/建议正确；历史与回看正常。

### B6 AI 对话

- `/api/health/chat`：规则引擎意图匹配（迁现有逻辑）；方案 B 后端直连大模型；对话记录写入 chat_history。
- 前端对话页：A/B 切换保留，历史恢复改从 `/api/health/chat` 会话读取（或按现有 localStorage 恢复方案，任选，推荐服务端存储以体现持久）。
- 删除 `vite.config.ts` 的 `/llm-api` 代理。
- **验收**：六类意图回答正确；无 Key 时 B 模式提示；重启服务后对话历史仍可恢复。

### B7 数据导入 + 演示数据

- `/api/health/records/import`（前端校验 + 服务端二次校验，返回 success/fail/errors）；`/api/health/records/export`；`/api/health/seed`（90 天数据 + 演示档案，逻辑从 mock 迁移）。
- 前端导入页、历史页导出、首页一键演示数据打通。
- **验收**：导入混合数据返回行级错误；seed 后 90 条记录、报告命中风险点。

### B8 用户管理 + 头像上传

- `/api/user` CRUD（admin）、`list-all-role/list-role-ids/dept`、`/api/upload`（multer）。
- 前端用户管理页、账户设置头像上传打通。
- **验收**：admin 可增删改用户（改密码 bcrypt 重哈希）；common 访问 /api/user 返回 403；上传头像返回可访问 URL。

### B9 Mock 移除 + 生产托管 + 部署准备

- 删除 `mock/` 目录、`vite-plugin-fake-server` 依赖、`build/plugins.ts` 中插件、`src/api/mock.ts` 中残留（formUpload 改真实 /api/upload）。
- Express 生产模式：`NODE_ENV=production` 时托管 `dist/` + SPA 回退（`app.get("*", ...)` 到 index.html，排除 /api）。
- 新增 `Dockerfile`（两阶段：build 前端 + node:22-alpine 运行，volume 挂载 `server/data`）与 `docker-compose.yml`（预留云端）。
- 根 `package.json`：`start: pnpm build && node server/src/index.ts`；README / 演示脚本更新（账号密码、双终端启动、本机演示说明）。
- **验收**：`pnpm build` 后 `pnpm start` 单端口访问全站；重启进程数据仍在；无 mock 残留（grep 确认）。

### B10 全链路回归

- `pnpm typecheck`、`pnpm build`、`pnpm start` 全流程；curl 全接口回归；admin/common 双账号权限走查；换浏览器/换设备（局域网 IP）数据一致；重启服务数据持久验证。
- **交付报告**：改动清单、验证结果、遗留问题。

---

## 七、部署

### 本机演示（比赛现场）

1. `pnpm install`（根 + server）
2. `pnpm dev`（前端 8848）+ `pnpm dev:server`（后端 3000）— 或生产模式 `pnpm start`（单端口 3000 全站）
3. 浏览器 `http://localhost:3000`，admin/admin123 登录 → 一键演示数据 → 演示全流程

> 说明：接入后端后"不依赖外网"仍成立（规则引擎在本地服务端、AI 方案 A 断外网可用），但**依赖本机服务进程**；比赛现场需提前启动服务。

### 云端部署（预留）

- `docker-compose up -d` 即可；`server/data` 挂载持久卷；环境变量：`JWT_SECRET`、`LLM_API_KEY`、`PORT`。
- 可选：Nginx 反代 443 + HTTPS；域名可选。

---

## 八、风险与注意

1. **commitlint**：subject 禁止大写拉丁字母开头，用中文或小写。
2. **better-sqlite3**：Node 22 有预编译二进制，Windows 一般直接可用；若安装失败需装 `windows-build-tools`。
3. **密码改动**：演示账号 admin123/common123 写进 README；评委可自行改密。
4. **token 兼容**：前端 http.ts 的刷新/登出逻辑依赖 `accessToken/refreshToken/expires` 三字段，后端返回结构必须保持。
5. **旧数据**：localStorage 中的健康数据不迁移；演示用 `/api/health/seed` 重新生成。
6. **端口**：后端默认 3000，被占用时改 `.env` 的 PORT 并同步 vite proxy target。
7. **删除 mock 后**：`pnpm lint:eslint` 的 `mock` 目录参数一并清理；`tsconfig` 中 mock 相关 include 检查。
8. **上传目录**：`server/data/uploads` 加入 gitignore；头像 URL 返回 `/uploads/xxx`（Express 静态托管）。

---

## 九、执行契约（供 AI 执行端遵守）

1. 每阶段独立 commit，subject 中性小写（如 `feat(server): 后端骨架`），可单独回退。
2. 删除/移动文件前先 `grep` 确认无引用；每阶段跑 `pnpm typecheck`（前端）+ `curl` 接口实测。
3. 不修改与本次无关的页面视觉、文案、框架核心；只动方案点名范围。
4. 规则引擎只能有一份源码（`server/shared/health-engine.ts`），禁止前端再复制一份完整实现。
5. 所有用户数据操作强制 `user_id` 隔离；SQL 参数化；密码 bcrypt。
6. 完成每阶段后输出：改动文件清单 + 验证结果 + 需用户知晓的决策；等待确认后再进入下一阶段。
7. 不删除 LICENSE、README、演示脚本；本方案文档保留在仓库中作为执行依据。
