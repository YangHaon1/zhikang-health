# 智康健康管理系统

一款面向个人健康管理的**纯前端 + mock 后端**演示系统。系统内置**本地医学规则引擎**作为 AI 内核（断网可用），并预留**真实大模型**通道（可切换），用于比赛现场演示健康数据的录入、分析、报告与智能对话全流程。

## 项目简介

- 系统名：**智康健康管理系统**
- 形态：纯前端 + mock 后端（开发为 Vite 中间件，生产 `enableProd` 走浏览器 XHook 注入），**无需部署后端、不依赖网络**
- AI 方案：**A+B 双通道**
  - 方案 A（默认）：本地医学规则引擎，稳定零成本、断网可用
  - 方案 B：真实大模型 API（火山方舟 / 豆包），经 Vite 反向代理转发
- 数据存储：个人手动录入 + 管理员 Excel 一键导入 + 演示数据一键生成
- 权限：RBAC（admin / common），动态路由菜单过滤

## 功能列表

| 模块              | 说明                                                                           |
| ----------------- | ------------------------------------------------------------------------------ |
| 健康总览          | 核心指标卡片（BMI / 血压 / 空腹血糖 / 综合风险评分）、近 30 天趋势、风险提示条 |
| 我的健康          | 个人健康档案（基本信息 + 生活方式）                                            |
| 指标管理          | 指标录入 + 历史记录（分页、日期筛选、修改、删除、导出）                        |
| 趋势分析          | ECharts 多指标趋势 + 正常参考范围带 + 时间范围筛选                             |
| AI 健康报告       | 一键生成风险评估报告（评分卡 + 雷达图 + 分项分析 + 建议）、导出 PDF / Excel    |
| AI 健康助手       | deep-chat 智能对话，基于真实健康数据回答健康问题                               |
| 数据导入（admin） | Excel 模板下载 / 上传 / 逐行校验 / 批量入库、一键生成演示数据                  |
| 用户管理（admin） | 系统用户管理                                                                   |

## 技术栈

- Vue 3 + Vite + TypeScript
- Element Plus + Tailwind CSS
- ECharts（指标趋势 / 雷达图）
- Pinia + vue-router
- deep-chat（AI 对话）、xlsx（Excel 导入导出）

## 启动方式

> 环境要求：Node ≥ 22.22.1、pnpm ≥ 11

```bash
# 安装依赖
pnpm install

# 开发模式（在 PowerShell / Windows Terminal 中运行）
pnpm dev

# 类型检查
pnpm typecheck

# 生产构建 + 本地预览
pnpm build
pnpm preview
```

## 账号说明

| 账号     | 密码 | 角色     | 菜单                               |
| -------- | ---- | -------- | ---------------------------------- |
| `admin`  | 任意 | 管理员   | 全部菜单（含数据导入、用户管理）   |
| `common` | 任意 | 普通用户 | 公共菜单（不含数据导入、用户管理） |

> mock 登录只校验用户名、不校验密码。

## 方案 B（真实大模型）Key 配置

默认使用**方案 A（本地规则引擎）**，无需任何配置、断网可用。如需接入真实大模型：

1. 在项目根目录新建 `.env.local`（该文件已被 `.gitignore` 忽略，不会提交），写入：

   ```ini
   VITE_LLM_API_KEY=你的火山方舟/豆包 API Key
   ```

2. 启动后在「AI 健康助手」页面切换到「真实大模型」模式（切换会写入 `localStorage["health-ai-mode"] = "llm"` 并刷新页面生效）。

> 说明：`server.proxy` 仅在开发环境生效；生产静态部署下方案 B 不可用，比赛现场默认使用方案 A 演示，断网也无碍。

## 项目结构（健康相关）

```
mock/health.ts                     # 健康数据 mock 接口 + 本地存储封装
src/api/health.ts                  # 前端 API 封装
src/utils/health-engine.ts         # 医学规则引擎（AI 方案 A 内核，mock 与前端共用）
src/types/health.ts                # 健康数据类型
src/views/health/
  ├─ dashboard/                    # 健康总览
  ├─ profile/                      # 我的健康（档案）
  ├─ records/                      # 指标管理（录入 + 历史）
  ├─ trend/                        # 趋势分析
  ├─ report/                       # AI 健康报告
  ├─ chat/                         # AI 健康助手
  └─ import/                       # 数据导入（admin）
```

## 许可证

[MIT](./LICENSE)
